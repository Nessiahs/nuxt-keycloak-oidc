#!/bin/sh

branch=$(git rev-parse --abbrev-ref HEAD)

if ! echo "$branch" | grep -E "^(feat|fix|chore|docs|refactor|test)/"; then
  echo "❌ Invalid branch name: $branch"
  echo "Use: feat/*, fix/*, chore/*, docs/*, refactor/*, test/*"
  exit 1
fi

